name: Tyme Directive Bridge

on:
  workflow_dispatch:
    inputs:
      directive_payload:
        description: "TYME Directive (DIRECTIVE, TARGET, INTENT)"
        required: true
        type: string

jobs:
  parse-and-route:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Parse Directive Envelope
        id: parse
        shell: bash
        run: |
          echo "Parsing directive payload..."

          PAYLOAD="${{ github.event.inputs.directive_payload }}"

          DIRECTIVE=$(echo "$PAYLOAD" | sed -n 's/^DIRECTIVE:[[:space:]]*//p' | head -n1)
          TARGET=$(echo "$PAYLOAD" | sed -n 's/^TARGET:[[:space:]]*//p' | head -n1)

          if [[ -z "$DIRECTIVE" || -z "$TARGET" ]]; then
            echo "❌ Missing DIRECTIVE or TARGET"
            exit 1
          fi

          echo "directive=$DIRECTIVE" >> $GITHUB_OUTPUT
          echo "target=$TARGET" >> $GITHUB_OUTPUT

          echo "✅ Parsed DIRECTIVE=$DIRECTIVE"
          echo "✅ Parsed TARGET=$TARGET"

      - name: Route Directive
        run: |
          echo "Routing directive..."

          case "${{ steps.parse.outputs.directive }}" in

            STRUCTURE)
              echo "🧱 STRUCTURE directive received"
              # call structure script or generator
              ;;
            
            TRANSFER)
              echo "📦 TRANSFER directive received"
              ;;
            
            ACTIVATE)
              echo "⚡ ACTIVATE directive received"
              ;;
            
            PROPAGATE)
              echo "🔁 PROPAGATE directive received"
              ;;
            
            SEAL)
              echo "🔒 SEAL directive received"
              ;;
            
            *)
              echo "❌ Unknown DIRECTIVE"
              exit 1
              ;;
          esac
